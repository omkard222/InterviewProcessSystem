default: &default
  adapter: mysql2
  encoding: utf8
  username: root
  password: root
  host: localhost
  port: 3306

development:
  <<: *default
  database: inteviewprocess_dev

test:
  <<: *default
  database: inteviewprocess_test
